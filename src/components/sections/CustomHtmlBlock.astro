---
interface Props {
  html: string;
}

const { html } = Astro.props;
---

{html.trim() && (
  <section class="section custom-html-block">
    <div class="container">
      <div class="cms-html" set:html={html} />
    </div>
  </section>
)}
