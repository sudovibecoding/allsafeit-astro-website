---
import LocationCards from '../../components/sections/LocationCards.astro';

interface Props {
  blok: {
    heading?: string;
  };
}

const { blok } = Astro.props;
---

<LocationCards heading={blok.heading} />
